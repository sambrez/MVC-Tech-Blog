<div class="card-header">
  <div class="card-title">
  <a href="/posts/{{id}}"><h2>{{post.header}}</h2><a/>
  <h5 class="card-subtitle">Posted by {{user.username}} on {{post.created_at}}</h5>
  </div>
  <div class="card-body">
    <p>{{post.body}}</p>
  </div>
</div>