<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>TECH BLOG</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css" />
</head>

<body>
  <header id="top" class="row">
    <h1 id="header" class="col">💻 TECH BLOG</h1>
    <div id="navbar" class="col">
      <nav id="nav">
        <a id="home" href="/">Home</a>
        <a id="dashboard" href="/dashboard">Dashboard</a>
        <!-- Only display the logout link if the user is logged in -->
        {{#if logged_in}}
        <a id="logout" href="/">Logout</a>
        {{else}}
        {{!-- if not display the login link --}}
        <a href="/login">Login</a>
        {{/if}}
      </nav>
    </div>
  </header>
  <main id="main" class="container container-fluid mt-5">
    {{{ body }}}
  </main>
  <footer>
    <p></p>
  </footer>
  <!-- Only include the logout script if the user is logged in -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}

</body>

</html>